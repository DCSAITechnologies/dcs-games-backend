PORT=8080
PAYMENTS_LIVE=0
# Supabase (durable persistence swap — see RUNBOOK). Service-role = server-side only, NEVER in chat/repo.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# CW4 netcode (separate WS service) — clients use this; not consumed by this HTTP server.
DCS_WS_URL=
